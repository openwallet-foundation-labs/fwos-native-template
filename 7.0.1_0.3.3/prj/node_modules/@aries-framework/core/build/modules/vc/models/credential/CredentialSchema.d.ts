export interface CredentialSchemaOptions {
    id: string;
    type: string;
}
export declare class CredentialSchema {
    constructor(options: CredentialSchemaOptions);
    id: string;
    type: string;
}
