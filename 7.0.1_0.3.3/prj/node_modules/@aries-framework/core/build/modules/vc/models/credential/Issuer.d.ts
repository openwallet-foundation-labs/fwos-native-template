import type { ValidationOptions } from 'class-validator';
/**
 * TODO: check how to support arbitrary data in class
 * @see https://www.w3.org/TR/vc-data-model/#credential-subject
 */
export interface IssuerOptions {
    id: string;
}
export declare class Issuer {
    constructor(options: IssuerOptions);
    id: string;
}
export declare function IssuerTransformer(): PropertyDecorator;
export declare function IsIssuer(validationOptions?: ValidationOptions): PropertyDecorator;
