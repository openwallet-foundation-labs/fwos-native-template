import type { EventEmitter } from '../../../../../../agent/EventEmitter';
import type { MessageHandler, MessageHandlerInboundMessage } from '../../../../../../agent/MessageHandler';
import { BatchMessage } from '../messages';
export declare class BatchHandler implements MessageHandler {
    private eventEmitter;
    supportedMessages: (typeof BatchMessage)[];
    constructor(eventEmitter: EventEmitter);
    handle(messageContext: MessageHandlerInboundMessage<BatchHandler>): Promise<void>;
}
